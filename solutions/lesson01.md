# 第 01 课答案要点

代码：`steps/lesson01_main.rs`

这一课只要改动了文字、程序能跑就算过。

**要能回答的**：

- 程序从 `fn main()` 开始跑。
- `cargo check` 只检查能不能编译（快），`cargo run` 编译并运行（慢一点）。写代码时多用前者。
- `println!` 的 `!` 说明它是宏。宏和函数的区别以后再讲，现在照着写。
